# Scaffold templates

Templates used by **create-extenzo-app**. Each subdirectory (e.g. `template-vanilla-ts`, `template-react-ts`, `template-uno-ts`) is a full project skeleton. The CLI downloads the chosen template from the GitHub repo tarball; ensure this `templates/` directory is committed and pushed at repo root.
