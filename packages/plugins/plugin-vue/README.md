# @extenzo/plugin-vue

Rsbuild plugin: enables Vue 3 (SFC, Vue JSX, Babel) for extenzo projects. Use in ext.config as `plugins: [vue()]`.

- Users: `import vue from "@extenzo/plugin-vue"; plugins: [vue()]`
- Depends on @rsbuild/plugin-vue, @rsbuild/plugin-babel, etc.
