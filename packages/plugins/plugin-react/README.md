# @extenzo/plugin-react

Rsbuild plugin: enables React and JSX for extenzo projects. Use in ext.config as `plugins: [react()]`; CLI injects @rsbuild/plugin-react before build.

- Users only need `import react from "@extenzo/plugin-react"; plugins: [react()]`
- Actual React support is provided by @rsbuild/plugin-react
